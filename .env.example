# Server Configuration
PORT=3001

# Network Configuration
# Options: mainnet, testnet
NETWORK=testnet

# Custom RPC URL (optional, will use default for selected network if not set)
# RPC_URL=https://aptos.testnet.porto.movementlabs.xyz/v1

# DEX Configuration
# Set your DEX contract address here
DEX_CONTRACT_ADDRESS=
DEX_MODULE_NAME=router

# Token Addresses
# Native MOVE token (APT equivalent on Movement)
MOVE_TOKEN=0x1::aptos_coin::AptosCoin

# USDC token address on Movement
# Get the correct address from the DEX you're using
USDC_TOKEN=

# Wallet Configuration (optional - can also be set via API)
# PRIVATE_KEY=your_private_key_hex

# Default TWAP Settings
DEFAULT_INTERVAL_MS=3600000
DEFAULT_SLIPPAGE_BPS=50
